"""
Restructured editor tab package with separate modules
"""
import logging
from .editor_tab import EditorTab

# Configure verbose logging for this module
logger = logging.getLogger('zimage.editor_tab')
logger.setLevel(logging.DEBUG)
